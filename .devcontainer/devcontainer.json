{
	"dockerFile": "Dockerfile",
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-dotnettools.csharp",
        "mongodb.mongodb-vscode"
			]
		}
	},
	"mounts": [
		{ "source": "nuget", "target": "/root/.nuget", "type": "volume" }
	],
	"containerEnv": {
    "MSSQL_SA_PASSWORD": "p@ssw0rd!",
    "MONGO_INITDB_ROOT_USERNAME": "sa",
    "MONGO_INITDB_ROOT_PASSWORD": "p@ssw0rd!"
	},
	"runArgs": [
		"--name=backend-storing",
    "--add-host=host.containers.internal:host-gateway",
    "--network=storing-network",
		"--privileged"
	],
  "postCreateCommand": "chmod u+x .devcontainer/*.sh; sh .devcontainer/creating.sh",
  "postStartCommand": "sh .devcontainer/starting.sh"
}
